# O TRIO DO FRAMEWORK ENTRA PINADO, NAS VERSOES QUE A PRODUCAO JA RODA. O
# resto deste arquivo continua solto de proposito — o que se quer travar aqui
# e o que ja quebrou, nao tudo.
#
# O QUE ACONTECEU EM 23/08/2026. Este arquivo nao tinha pino nenhum, e o
# `Dockerfile` constroi a partir dele: cada `docker compose up --build`
# instalava o que o pip resolvesse NAQUELE DIA. Foi assim que a VPS chegou ao
# `starlette 1.6.0` sem ninguem ter escolhido — e foi assim que se descobriu,
# porque no starlette 1.6 o `include_router` DEIXOU DE COPIAR as rotas para
# `app.routes`: onde havia 126 objetos `Route` passou a haver 28, sendo 24
# `_IncludedRouter`.
#
# A API nao quebrou com isso, e e o que torna o caso perigoso: o app sobe, as
# rotas respondem, os pedidos entram, o `app.openapi()` sai completo com os
# 100 caminhos. Quem parou foi a INTROSPECCAO — e com ela
# `tests/test_papeis_das_rotas.py`, a auditoria que confere o papel exigido
# por cada rota /admin. A trava que impede rota nova de nascer aberta ao
# `print_agent` ficou desligada em producao ate o commit que acompanha estes
# pinos, que ensinou os tres testes a atravessar o `_IncludedRouter`.
#
# POR QUE O PINO E NA VERSAO NOVA, E NAO NA ANTIGA. A primeira tentativa
# pinou o trio na versao da maquina de desenvolvimento (0.116.1 / 0.47.3 /
# 2.11.7), que era a que passava nos testes. Estava errado: a producao ja
# rodava a nova, e o pino a faria DESCER tres frameworks no proximo
# `--build`. Pino nao e escolha de versao — e o registro do que ESTA no ar.
# Trocar de versao e outro trabalho, com os testes indo junto.
#
# `starlette` aparece aqui embora seja dependencia transitiva do FastAPI: e
# ELE que muda o comportamento, e deixa-lo implicito seria pinar a causa pelo
# sintoma.
#
# Subir esses tres continua sendo um commit deliberado, com o que introspecta
# rota conferido junto e o repositorio ficando verde no mesmo commit.
fastapi==0.141.1
starlette==1.6.0
# Exigida pelo FastAPI para ler multipart/form-data. Sem ela, a rota de
# upload de imagem do painel (POST /admin/products/{id}/image) derruba o
# boot com "Form data requires python-multipart".
python-multipart
uvicorn[standard]
sqlalchemy
psycopg[binary]
pydantic==2.13.4
pydantic-settings
python-dotenv
resend
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT
langchain-core
langchain-openai
httpx
redis
psycopg2-binary
slowapi
# >=1.16 pelo if_not_exists/if_exists de create_index/drop_index, que as
# migracoes sobre as tabelas do baseline usam.
alembic>=1.16
cryptography
