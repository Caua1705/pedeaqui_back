# O config.ini de verdade guarda a senha do lojista. So o .example vai para
# o git.
config.ini

# Estado e log sao da maquina, nao do projeto.
state/
logs/
*.log
# Fica na raiz do agente, ao lado do .exe, e nao dentro de state/ (paths.py:
# o lojista e instruido por telefone a abrir a pasta), entao state/ nao pega.
pedidos-impressos.json

__pycache__/
*.py[cod]
.pytest_cache/
venv/
.venv/
